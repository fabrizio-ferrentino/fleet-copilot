# Mosquitto configuration for local development only.
# Anonymous access is acceptable here because the broker is never exposed
# beyond the developer machine / compose network (see spec non-goals: no auth).
listener 1883
allow_anonymous true

# Telemetry is a continuous stream; losing queued messages on broker restart
# is acceptable for this project, so no persistence to disk.
persistence false

log_dest stdout
